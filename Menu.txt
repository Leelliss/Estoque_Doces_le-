public class Program
{
    static void Main(string[] args)

    {
        
        ControleEstoque estoque = new();  
        bool continuar = true;
        while (continuar)
        
    public int Menu  ()
    {
                Console.WriteLine(@"Escolha uma opção:
                    1. Novo Produto
                    2. Listar Produtos
                    3. Remover Produto
                    4. Entrada de Estoque
                    5. Saída de Estoque
                    0. Sair");

            return Menu;

        switch Menu
        {
                case 1:
            //adicionando produto
            Console.Write("Nome do Produto: ");
                    string nome = Console.ReadLine();
            
            Console.Write("Sabor do Produto: ");
                    string sabor = Console.ReadLine();
            
            Console.Write("Preço: ");
                        if (Decimal.TryParse(Console.ReadLine(), out preco))
                            break;
            Console.WriteLine("Formato inválido. Por favor, digite um valor decimal válido.");

            Console.Write("Modelo do Produto: ");
                    string modelo = Console.ReadLine();
            
                case 2:
                    // Listar todos os produtos
            estoque.ListarProdutos();
                    break;

                case 3:
                    // Remover um produto pelo ID
                int idRemover;
                    while (true)
                    {
            Console.Write("ID do Produto a Remover: ");
                if (Int32.TryParse(Console.ReadLine(), out idRemover))
                            break;
            Console.WriteLine("Formato inválido. Por favor, digite um número inteiro válido.");
                    }
                    estoque.RemoverProduto(idRemover);
                    break;

                case 4:
                    // Entrada de produtos no estoque
                int idEntrada, quantidadeEntrada;
                while (true)
                    {
            Console.Write("ID do Produto para Entrada em Estoque: ");
                if (Int32.TryParse(Console.ReadLine(), out idEntrada))
                     break;
            Console.WriteLine("Formato inválido. Por favor, digite um número inteiro válido.");
                    }
                while (true)
                    {
            Console.Write("Quantidade a Adicionar: ");
                        if (Int32.TryParse(Console.ReadLine(), out quantidadeEntrada))
                            break;
            Console.WriteLine("Formato inválido. Por favor, digite um número inteiro válido.");
                    }
                    estoque.EntradaEstoque(idEntrada, quantidadeEntrada);
                    break;

                case 5:
                    // Saída do estoque
                    int idSaida, quantidadeSaida;
                    while (true)
                    {
            Console.Write("ID do Produto para Saída de Estoque: ");
                        if (Int32.TryParse(Console.ReadLine(), out idSaida))
                            break;
             Console.WriteLine("Formato inválido. Por favor, digite um número inteiro válido.");
                    }
                    while (true)
                    {
            Console.Write("Quantidade a Remover: ");
                        if (Int32.TryParse(Console.ReadLine(), out quantidadeSaida))
                            break;
            Console.WriteLine("Formato inválido. Por favor, digite um número inteiro válido.");
                    }
                    estoque.SaidaEstoque(idSaida, quantidadeSaida);
                    break;

                case 0:
                    // Sair do programa
                    continuar = false;
                    Console.WriteLine("Encerrando o programa...");
                    break;

                default:
                    // Opção inválida
                    Console.WriteLine("Opção inválida. Tente novamente.");
                    break;
                         }
        }
    }
}